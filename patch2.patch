diff --git a/src/core/convexHull.ts b/src/core/convexHull.ts
new file mode 100644
--- /dev/null
+++ b/src/core/convexHull.ts
@@
+/**
+ * 2D 点集合の凸包をモノトーンチェーンで求める
+ * 返値は頂点インデックス配列（反時計回り、終点重複なし）
+ */
+export function computeConvexHull(points: [number, number][]): number[] {
+  const idx = points.map((_, i) => i);
+  idx.sort((i, j) => points[i][0] - points[j][0] || points[i][1] - points[j][1]);
+
+  const cross = (i: number, j: number, k: number) =>
+    (points[j][0] - points[i][0]) * (points[k][1] - points[i][1]) -
+    (points[j][1] - points[i][1]) * (points[k][0] - points[i][0]);
+
+  const lower: number[] = [];
+  for (const i of idx) {
+    while (lower.length >= 2 &&
+           cross(lower[lower.length - 2], lower[lower.length - 1], i) <= 0) {
+      lower.pop();
+    }
+    lower.push(i);
+  }
+
+  const upper: number[] = [];
+  for (let p = idx.length - 1; p >= 0; p--) {
+    const i = idx[p];
+    while (upper.length >= 2 &&
+           cross(upper[upper.length - 2], upper[upper.length - 1], i) <= 0) {
+      upper.pop();
+    }
+    upper.push(i);
+  }
+
+  upper.pop();
+  lower.pop();
+  return lower.concat(upper);
+}
+
+/** 凸包上の辺か（順不同） */
+export function isHullEdge(hull: number[], u: number, v: number): boolean {
+  for (let i = 0; i < hull.length; i++) {
+    const j = (i + 1) % hull.length;
+    if ((hull[i] === u && hull[j] === v) || (hull[i] === v && hull[j] === u)) return true;
+  }
+  return false;
+}
diff --git a/tests/constraints.test.ts b/tests/constraints.test.ts
new file mode 100644
--- /dev/null
+++ b/tests/constraints.test.ts
@@
+import { describe, it, expect } from 'vitest';
+import { segmentsIntersect } from '../src/core/geometry';
+import { computeConvexHull, isHullEdge } from '../src/core/convexHull';
+import { hasEdge, flipEdge } from '../src/core/edgeOps';
+import { triangulate, Point } from '../src/triangulate'; // (次フェーズ公開 API を想定)
+
+describe('geometry utils', () =>
